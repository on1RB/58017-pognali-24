form.companion(action="https://echo.htmlacademy.ru" method="post")
  h2._caption Подберите идеального попутчика
  fieldset._items.-hobbies
    button._interest.-active(type="button") Хобби:
    ul.interest-list.-active._list
      li._item.-first
        label._control
          input._input.visually-hidden(type="checkbox" name="gym" checked)
          span._mark
          span._label Спортзал
      li._item.-second
        label._control
          input._input.visually-hidden(type="checkbox" name="hooka")
          span._mark
          span._label Кальян
      li._item.-third
        label._control
          input._input.visually-hidden(type="checkbox" name="sofa")
          span._mark
          span._label Диван
  fieldset._items.-music
    button._interest.-active(type="button") Музыка:
    ul.interest-list.-active._list
      li._item.-first
        label._control
          input._input.visually-hidden(type="checkbox" name="hard-rock" checked)
          span._mark
          span._label Тяжелый рок
      li._item.-second
        label._control
          input._input.visually-hidden(type="checkbox" name="russian-rap")
          span._mark
          span._label Русский рэп
      li._item.-third
        label._control
          input._input.visually-hidden(type="checkbox" name="eurodance")
          span._mark
          span._label Евроденс
  fieldset._items.-eat
    button._interest.-active(type="button") Еда:
    ul.interest-list.-active._list
      li._item.-first
        label._control
          input._input.visually-hidden(type="checkbox" name="meat-eater" checked)
          span._mark
          span._label Мясоед
      li._item.-second
        label._control
          input._input.visually-hidden(type="checkbox" name="good-nutrition")
          span._mark
          span._label Сидит на ПП
      li._item.-third
        label._control
          input._input.visually-hidden(type="checkbox" name="vegan")
          span._mark
          span._label Веган-сыроед
  fieldset._items.-transport
    button._interest.-active(type="button") Транспорт:
    ul.transport-list.-active.-catalog._list
      li._item
        label._control
          input._input.visually-hidden(type="checkbox" name="plane" checked)
          span._mark.-plane
          span._label.visually-hidden Авиаперевозки
      li._item
        label._control
          input._input.visually-hidden(type="checkbox" name="bus" checked)
          span._mark.-bus
          span._label.visually-hidden Автобусные путешествия
      li._item
        label._control
          input._input.visually-hidden(type="checkbox" name="bicycle")
          span._mark.-bicycle
          span._label.visually-hidden Кататься на велосипеде
      li._item
        label._control
          input._input.visually-hidden(type="checkbox" name="run" checked)
          span._mark.-run
          span._label.visually-hidden Пешие прогулки
  fieldset._items.-level(aria-expanded="true")
    button._interest.-active Левел:
    ._range-wrapper.-active
      label._label
        span.visually-hidden#range-min Минимальный
        input._level.-min(type="number" name="min-level" value=RANGE_SLIDER.valueA min=RANGE_SLIDER.min, max=RANGE_SLIDER.max)
      label._label
        span.visually-hidden#range-max Максимальный
        input._level.-max(type="number" name="max-level" value=RANGE_SLIDER.valueB min=RANGE_SLIDER.min, max=RANGE_SLIDER.max)
    ._range.-active
      input._line.-min(type="range", value=RANGE_SLIDER.valueA min=RANGE_SLIDER.min, max=RANGE_SLIDER.max aria-labelledby='range-min')
      input._line.-max(type="range", value=RANGE_SLIDER.valueB min=RANGE_SLIDER.min, max=RANGE_SLIDER.max aria-labelledby='range-max')
  button.button-show.-bg(type="button") Показать
